{
  "name": "react-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist/assets && cp -r public/* dist/ && cp index.html dist/",
    "dev": "vite --port 3000",
    "lint": "echo 'No linting needed'"
  },
  "dependencies": {
    "jimp": "^1.6.1",
    "vite": "^5.0.0"
  }
}
